services:
  server:
    platform: linux/amd64
    image: sajibsv/elad_giserman_server:latest
    container_name: elad_giserman_api
    restart: always
    environment:
      NODE_ENV: 'production'
      DATABASE_URL: 'postgresql://postgres:postgres@db:5432/elad_giserman_db'

      JWT_SECRET: 'secret'
      JWT_EXPIRES_IN: 90d

      BASE_URL: 'https://lgcglobalcontractingltd.com/js'
      PORT: 5060

      MAIL_USER: 'arafatmannan2019@gmail.com'
      MAIL_PASS: 'eurp zvyi gnoy enag'

      REDIS_HOST: redis
      REDIS_PORT: 6379

      SUPER_ADMIN_EMAIL: 'sajib.softvence@gmail.com'
      SUPER_ADMIN_PASS: 'superadmin'

      OAUTH_CLIENT_ID: '1074646902032-5ab3gkkggb7mrejjfnvr7ra65qos4o8s.apps.googleusercontent.com'
      OAUTH_CLIENT_SECRET: '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'

    ports:
      - 5060:5060
    depends_on:
      - db
      - redis
    command: >
      sh -c "
        npx prisma migrate deploy &&
        pnpm start
      "
    volumes:
      - files-data:/app/uploads

  db:
    image: postgres
    container_name: elad_giserman_db
    restart: always
    user: postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: elad_giserman_db
    volumes:
      - elad_giserman_data:/var/lib/postgresql/data
    ports:
      - '5448:5432'

  redis:
    image: redis
    container_name: jesus_redis
    restart: always
    ports:
      - '6383:6379'

volumes:
  elad_giserman_data:
  files-data:
