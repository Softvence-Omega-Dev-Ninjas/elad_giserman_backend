model CustomApp {
  id          String   @id @default(uuid())
  title       String?
  description String?
  logo        String?
  bannerCard  String?
  bannerPhoto String?
  createAt    DateTime @default(now())
  updatedAt   DateTime @updatedAt
}
